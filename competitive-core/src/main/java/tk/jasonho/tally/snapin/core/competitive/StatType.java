package tk.jasonho.tally.snapin.core.competitive;

/**
 * Must be exactly as in db.
 */
public enum StatType {
    KILL,
    DEATH,
    WIN,
    LOSS,
    PLAYED,
    OBSERVE,
    ASSIST,
    INCONCLUSIVE
}
